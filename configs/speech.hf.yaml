transcription:
    engine: "parakeet"
    config: "parakeet"
    device: "cpu"

alignment:
    enabled: false
    engine: "none"
    config: "wav2vec2"
    device: "cpu"

diarization:
    enabled: false
    engine: "none"
    config: "diarization-community"
    device: "cpu"

summarization:
    config: "qwen"
