transcription:
    engine: "parakeet"
    config: "parakeet"
    device: "cpu"

alignment:
    enabled: true
    engine: "wav2vec2_external"
    config: "wav2vec2"
    device: "cpu"

diarization:
    enabled: true
    engine: "community_external"
    config: "diarization-community"
    device: "cpu"

summarization:
    config: "qwen"
